<div>
  <div
    class="text-center p-3 rounded-4 position-center login-color max-width-login login"
  >
    <div class="form-sign m-auto">
      <form
        id="actualizar-password-form"
        action="/actualizar-password"
        method="post"
      >
        <input type="hidden" name="UsuarioId" value={{usuarioId}} />
        <input type="hidden" name="PasswordToken" value={{passwordToken}} />

        <div class="d-flex gap-2 flex-column mt-3">
          <div class="form-floating">
            <input
              type="password"
              class="form-control input"
              id="password"
              placeholder="Password"
              name="Password"
            />
            <label for="floatingPassword" class="text-dark">Contraseña</label>
            <p class="text-danger fst-italic text-start d-none mb-0">Este campo
              es requerido</p>
            <span
              class="text-danger fst-italic text-start d-none non-equal-password mb-0"
            >las contraseñas no son iguales</span>
          </div>
          <div class="form-floating">
            <input
              type="password"
              class="form-control input"
              id="confirm-password"
              placeholder="Password"
              name="ConfirmPassword"
            />
            <label for="floatingConfirmPassword" class="text-dark">Confirmar
              Contraseña</label>
            <p class="text-danger fst-italic text-start d-none mb-0">Este campo
              es requerido</p>
            <span
              class="text-danger fst-italic text-start d-none non-equal-password mb-0"
            >las contraseñas no son iguales</span>
          </div>
        </div>

        <button
          class="w-100 btn btn-lg btn-primary text-light mt-2"
          type="submit"
          id="btn-actualizar-password"
        >Confirmar cambio</button>

        <div class="mt-2">
          <a
            class="w-100 btn btn-lg btn-warning text-light"
            href="/resetear-password"
            id="btn-volver-atras"
          >Volver atras</a>
        </div>
      </form>
    </div>
  </div>
</div>